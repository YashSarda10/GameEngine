import { GameSystem } from './GameSystem.js';

export class RenderSystem implements GameSystem {
    public init(): void {
        // Setup canvas, camera, etc.
    }

    public update(): void {
        // Draw frame
    }
}

