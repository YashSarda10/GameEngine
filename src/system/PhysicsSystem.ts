import { GameSystem } from './GameSystem.js';

export class PhysicsSystem implements GameSystem {
    public init(): void {
        // Initialize physics engine, alloc buffers etc.
    }

    public update(): void {
        // Simulate physics step
    }
}

